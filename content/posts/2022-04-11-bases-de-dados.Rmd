---
title: "Bases de dados: para praticar e ensinar"
date: "2022-04-11"
tags: ["ensino", "bases de dados", "importação"]
categories: ["tutoriais"]
#image: ""
author: ["Beatriz", "Tereza"]
summary: "Quer saber como acessar várias bases de dados com R?"
draft: true
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  message = FALSE, 
  warning = FALSE, 
  collapse = TRUE
)
```

## Introdução

Neste post, listamos alguns lugares onde encontrar bases de dados interessantes para quem está aprendendo e quer praticar com novas bases, e também para quem quer ensinar R e procura bases de dados para criar exemplos!

O post será separado em: bases em português (sejam elas traduzidas ou originais), e em inglês.

## Bases de dados em Português

### Pacote dados

O pacote está **disponível no CRAN** e pode ser instalado utilizando o seguinte código:

```r
install.packages("dados")
```

[Escrevemos um post recentemente focado neste pacote, então recomendamos checar lá](https://blog.curso-r.com/posts/2022-04-06-pacote-dados/)! Para acessar as bases de dados, utilize a função `library(dados)` ou use com `dados::nome_da_base`. Por exemplo:


```{r}
library(dados)
dplyr::glimpse(dados::pixar_filmes)
```


### Pacote de bases usadas em cursos da Curso-R

Algumas bases de dados foram traduzidas por pessoas da Curso-R, e disponibilizadas nesse pacote para serem usadas nos cursos. Esse pacote não está disponível no CRAN, então caso queira instalar, utilize os seguintes comandos:


```r
install.packages("devtools")
devtools::install_github("curso-r/basesCursoR")
```

Para verificar quais são as bases disponíveis no pacote, utilize a seguinte função: `basesCursoR::bases_disponiveis()`.

```{r}
basesCursoR::bases_disponiveis()
```

Para carregar alguma base do pacote, utilize a função `basesCursoR::pegar_base("nome_da_base")`, por exemplo:

```{r}
pokemon <- basesCursoR::pegar_base("pokemon")
dplyr::glimpse(pokemon)
```


### Base dos dados

ESCREVER!


O base dos dados tem bases grandes e "da vida real!"
https://staging.basedosdados.org/dataset?resource_type=bdm_table&order_by=score

https://blog.curso-r.com/posts/2021-05-28-basedosdados/
https://www.youtube.com/watch?v=8D4jK-YCxLU&t=2688s




## Bases de dados em Inglês

### Bases do tidytuesday

ESCREVER!


https://github.com/rfordatascience/tidytuesday#datasets

```{r}
jogos_ratings <- readr::read_csv('https://raw.githubusercontent.com/rfordatascience/tidytuesday/master/data/2022/2022-01-25/ratings.csv')

dplyr::glimpse(jogos_ratings)
```




###  Pacote ISLR 

O pacote ISLR acompanha o livro [Introduction to Statistical Learning](https://www.statlearning.com/) (disponível [gratuitamente aqui](https://hastie.su.domains/ISLR2/ISLRv2_website.pdf)). Apresenta bases de dados interessantes para treinar modelagem, acompanhando os exemplos do livro. 

Você pode instalar o pacote através do CRAN, usando a função `install.packages()`:

```r
install.packages("ISLR")
```

Essas são as bases de dados disponíveis no pacote, que podem ser acessadas utilizando a função `library(ISLR)` ou use com `ISLR::nome_da_base`.  :

```{r echo=FALSE}
bases <- data(package = 'ISLR')

tibble::as_tibble(bases$results) |>
  
  dplyr::mutate(Nome = stringr::str_remove(Item, " \\(data\\)")) |>
  dplyr::select(Nome, `Descrição` = Title) |>
  knitr::kable() |>
  kableExtra::kable_material(c("striped", "hover"))
```


### Kaggle

ESCREVER!

https://www.kaggle.com/datasets




## Conclusão

Esperamos que este post seja interessantes para quem está aprendendo e quer praticar com novas bases, e também para quem quer ensinar R e procura bases de dados para criar exemplos! Bons estudos e até a próxima!
